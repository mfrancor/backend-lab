info:
  name: Get produto
  type: http

http:
  method: GET
  url: "{{baseUrl}}/produto/{{id-produto}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
